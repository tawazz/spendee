version: "3"
services:
  web:
    image: spendee
    deploy:
      replicas: 4
      restart_policy:
        condition: on-failure
    ports:
      - "8005:80"
    networks:
      - webnet
networks:
  webnet:
